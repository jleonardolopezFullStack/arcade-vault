
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Contraseña de conexión directa a Postgres (CLI o cliente SQL).
# La aplicación NO la lee. Panel de Supabase > Project Settings > Database.
SUPABASE_DB_PASSWORD=

# Panel de Supabase > Project Settings > API (o `supabase` MCP: get_project_url).
NEXT_PUBLIC_SUPABASE_URL=
# Clave publishable `sb_publishable_...`, no la `anon` heredada.
# Panel de Supabase > Project Settings > API Keys (o MCP: get_publishable_keys).
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
